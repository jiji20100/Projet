<?xml version="1.0" encoding="utf-8"?>
<navigation
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.example.daricom.view.HomeFragment"
        android:label="HomeFragment"
        tools:layout="@layout/fragment_home">
        <action
            android:id="@+id/action_homeFragment_to_estimmoFragment"
            app:destination="@id/estimmoFragment" />
    </fragment>
    <fragment
        android:id="@+id/estimmoFragment"
        android:name="com.example.daricom.view.EstimmoFragment"
        android:label="EstimmoFragment"
        tools:layout="@layout/fragment_estimmo">
    </fragment>


</navigation>